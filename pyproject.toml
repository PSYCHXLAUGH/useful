[tool.poetry]
name = "useful"
version = "0.1.0"
description = ""
authors = ["Test <psychx@psychx.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.32.3"
loguru = "^0.7.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
